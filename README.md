# Cross_Development_3_Electron
Cross-platform movietracker built with Ionic/Angular and Electron

In order to run the project:
```
npm install --legacy-peer-deps
npm run build
npm start
```

For building the executable file, run:
```
npm run make
```